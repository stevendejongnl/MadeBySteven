import { LitElement, html } from 'lit'
import { customElement } from 'lit/decorators.js'

@customElement('mbs-projects-page')
export class MbsProjectsPage extends LitElement {
  override render() {
    return html`<section>Projects page placeholder</section>`
  }
}
