import { LitElement, html } from 'lit'
import { customElement } from 'lit/decorators.js'

@customElement('mbs-contact-page')
export class MbsContactPage extends LitElement {
  override render() {
    return html`<section>Contact page placeholder</section>`
  }
}
